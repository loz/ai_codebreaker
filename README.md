# Tensorflow Codebreaker

Codebreaker game which provides clues for guessing coloured (numbered) code.

# Environment

  virtualenv ./venv && source ./venv/bin/activate && pip install venv.txt


# Saving Dependencies

  python freeze > venv.txt
